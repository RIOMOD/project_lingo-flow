# Production profile - Render.com + Neon PostgreSQL
# Activated automatically when SPRING_PROFILES_ACTIVE=prod

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-jolly-math-a77g7ay6-pooler.c-3.ap-southeast-1.aws.neon.tech/neondb?sslmode=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_6wX3zMdHuLrt}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-test-query: SELECT 1
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          lob:
            non_contextual_creation: true
    show-sql: false
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 1
    repair-on-migrate: true
    validate-on-migrate: false
    locations: classpath:db/migration
    out-of-order: true

logging:
  level:
    com.example: INFO
    org.flywaydb: DEBUG
    org.springframework.security: WARN
